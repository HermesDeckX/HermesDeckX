const n="会话自动重置",e="配置会话自动重置策略，让 AI 助手每天「清零」或在空闲后自动重置，保持清爽",s={body:`## 为什么需要自动重置？

长时间的对话会积累大量上下文，导致：
- AI 回复速度变慢（处理更多历史信息）
- Token 消耗增加
- 上下文混乱（多个话题混在一起）

自动重置可以定期清空对话历史，让每次对话从头开始。

## 重置模式

| 模式 | 说明 |
|------|------|
| **daily** | 每天在指定小时自动重置 |
| **idle** | 空闲超过指定分钟数后重置 |

## 在 HermesDeckX 中配置

前往「配置中心 → 会话」→ 找到「自动重置」区域：

1. 选择重置模式（daily 或 idle）
2. daily 模式：设置 \`atHour\`（0-23，如 6 表示凌晨 6 点重置）
3. idle 模式：设置 \`idleMinutes\`（如 120 表示 2 小时无消息后重置）

## 按对话类型设置不同策略

可以为不同类型的对话设置不同的重置策略：

- **direct** — 私聊会话
- **group** — 群组会话
- **thread** — 主题/帖子会话

前往「配置中心 → 会话 → 按类型重置」分别配置。

## 按频道设置不同策略

也可以为不同频道设置独立的重置策略。例如：
- Telegram 使用 daily 模式（每天重置）
- Discord 使用 idle 模式（2 小时无消息后重置）

前往「配置中心 → 会话 → 按频道重置」配置。

## 手动重置

随时可以在聊天中发送 \`/new\` 命令手动重置当前会话，或发送 \`/compact\` 手动压缩上下文。

## 配置字段

对应配置路径：\`session.reset\`

\`\`\`json
"session": {
  "reset": {
    "mode": "daily",
    "atHour": 6
  }
}
\`\`\``},o={name:n,description:e,content:s};export{s as content,o as default,e as description,n as name};
